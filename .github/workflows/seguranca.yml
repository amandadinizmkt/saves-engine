name: Seguranca

# Receita central em DivosdaIA/infra - ajuste la, vale para todos.
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write

jobs:
  revisar:
    uses: DivosdaIA/infra/.github/workflows/revisao-seguranca.yml@main
    secrets: inherit
