name: Código

# Receita central em DivosdaIA/infra — ajuste lá, vale para todos.
on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  revisar:
    uses: DivosdaIA/infra/.github/workflows/revisao-codigo.yml@main
    secrets: inherit
    with:
      profundidade: high
